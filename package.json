{
  "name": "forge",
  "version": "0.1.0",
  "private": true,
  "description": "Goal-based habit and task tracker structured around long-term missions.",
  "scripts": {
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build:client": "npm --prefix client run build",
    "build:server": "npm --prefix server run build",
    "install:all": "npm install --prefix client && npm install --prefix server"
  },
  "keywords": [
    "productivity",
    "gamification",
    "habits",
    "goals",
    "fullstack",
    "react",
    "express",
    "postgresql",
    "prisma",
    "typescript"
  ],
  "author": "",
  "license": "MIT"
}
